[package]
name = "demo-validator"
version = "0.1.0"
edition = "2021"

[dependencies]
core = { path = "../../lib/core" }
validator = { path = "../../lib/validator" }


secp256k1 = { workspace = true }
